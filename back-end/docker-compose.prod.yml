services:
  postgres:
    # DB port is NOT exposed — only the api service can reach it via the internal network
    restart: always

  api:
    ports:
      - "${PORT:-3000}:${PORT:-3000}"
    restart: always
    deploy:
      resources:
        limits:
          memory: 512M
